fastapi==0.141.1
uvicorn[standard]==0.52.1
spotipy==2.26.0
yt-dlp>=2026.7.4
mutagen==1.48.1
# python-multipart 0.0.6 → 0.0.26: CVE-2024-24762 (HIGH, ReDoS),
# CVE-2024-53981 (DoS), CVE-2026-24486 (Path-Traversal), CVE-2026-40347 (DoS)
python-multipart==0.0.32
# requests 2.31.0 → 2.33.0: CVE-2024-35195 (cert-verify-bypass),
# CVE-2024-47081 (.netrc credential leak), CVE-2026-25645 (security bypass)
requests==2.34.2
aiofiles==25.1.0
pydantic==2.13.4
# python-dotenv 1.0.0 → 1.2.2: CVE-2026-28684 (arbitrary file overwrite via symlink)
python-dotenv==1.2.2
ytmusicapi==1.12.1
yt-dlp-get-pot-rustypipe==0.2.0
# curl-cffi: aktiviert yt-dlp's impersonate-Option (TLS-Handshake-Spoofing
# via libcurl-impersonate) für robustere Anti-Bot-Detection.
# Pinning an yt-dlp's RELEASE-supported-range koppeln (nicht master!):
# yt-dlp 2026.03.17 (aktuellstes Release) erlaubt nur 0.5.10 + 0.10.x–0.14.x
# (yt_dlp/networking/_curlcffi.py: "< (0, 15)"). master erlaubt schon <0.16
# — sobald das nächste yt-dlp-Release (>2026.03.17) raus ist, auf
# >=0.15,<0.16 bumpen (löst CVE-2026-33752-ignore auf, siehe v0.4.7/v0.4.8-
# CHANGELOG für die Lessons). Eine Version außerhalb der Range lädt die
# Bridge nicht: "Impersonate target ... is not available".
curl-cffi>=0.10,<0.15
# ─── Auth (Phase F) ──────────────────────────
argon2-cffi==25.1.0
pyotp==2.10.0
qrcode==8.2
# PyJWT 2.8.0 → 2.12.0: CVE-2026-32597 (HIGH, RFC8725 §3.10 violation —
# accepts unknown crit header extensions)
PyJWT==2.13.0
# cryptography 42.0.5 → 46.0.5: CVE-2026-26007 (subgroup-attack),
# CVE-2024-12797 (OpenSSL RFC7250), CVE-2026-34073 (security bypass),
# bundled-OpenSSL vulns (GHSA-h4gh-qq45-vh27)
# → 48.0.1: GHSA-537c-gmf6-5ccf (vulnerable OpenSSL in den Wheels)
# → 50.0.0: CVE-2026-69247 / PYSEC-2026-3552 (PKCS#7-EnvelopedData-Decryption
#   gibt über unterscheidbare Fehler + Timing ein Bleichenbacher-Oracle preis).
#   Tonus nutzt nur Fernet (utils/app_settings.py, utils/auth_users.py), der
#   verwundbare Pfad ist nicht erreichbar — Bump trotzdem, weil der Audit die
#   installierte Version misst, nicht die erreichbaren Codepfade. Fernet-API
#   ist in 50.0.0 unverändert, die Breaking Changes betreffen X.509-Verify.
cryptography==50.0.0
